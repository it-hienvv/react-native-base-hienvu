import {connect} from 'react-redux';
import Opportunity from './index';

const mapsStateToProps = state => ({});

const mapsDispatchToProps = dispatch => ({});

export default connect(mapsStateToProps, mapsDispatchToProps)(Opportunity);
