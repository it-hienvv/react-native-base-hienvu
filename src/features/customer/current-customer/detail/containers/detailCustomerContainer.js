import {connect} from 'react-redux';
import ExCustomer from './index';

const mapsStateToProps = state => ({});

const mapsDispatchToProps = dispatch => ({});

export default connect(mapsStateToProps, mapsDispatchToProps)(ExCustomer);
